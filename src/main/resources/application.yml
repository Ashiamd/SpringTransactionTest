spring:
  #数据库配置
  datasource:
    #数据库连接池类型
#    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test?useUnicode=true&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: example

mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    root: info
  file:
    path: logs